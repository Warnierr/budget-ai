# Base de données (Neon/PostgreSQL)
DATABASE_URL="postgresql://user:password@localhost:5432/budgetai"

# NextAuth.js
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="change-me-with-openssl-rand-hex-32"

# IA (OpenRouter recommandé)
OPENROUTER_API_KEY="sk-or-v1-..."
# Option si vous stockez la clé dans un fichier (config/openrouter.key)
# OPENROUTER_API_KEY_FILE="config/openrouter.key"

# OAuth Google (optionnel)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# OpenAI (optionnel si vous ne passez pas par OpenRouter)
OPENAI_API_KEY=""

# Chiffrement des données sensibles (si vous en avez besoin)
ENCRYPTION_KEY="your-32-bytes-hex-key-here"

# Environnement
NODE_ENV="development"
